1. When saving assemblies and selecting icons, the system's catalog of icons is now searchable.

2. The "Text Default if Empty" module will now create the destination node if it does not exist, so long as the immediate parent node does exist.

3. The system can now perform its license key check to the license server at apiant.com, which was modified on 3/23/18 to use an SSL certificate from Let's Encrypt.